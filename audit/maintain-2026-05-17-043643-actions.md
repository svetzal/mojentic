All done. Here's a summary of what happened:

**Dependency update — mojentic-py**

Only one package was outdated:
- `click` 8.3.3 → 8.4.0 (transitive dependency, minor bump)

All direct dependencies in `pyproject.toml` were already at their latest compatible versions.

**Quality gates — all passed:**
- ✅ Lint (`flake8`): 0 issues
- ✅ Tests (`pytest`): 244 passed in 1.92s
- ✅ Security audit (`pip-audit`): No known vulnerabilities

The updated `uv.lock` was committed and pushed to `origin/main`.