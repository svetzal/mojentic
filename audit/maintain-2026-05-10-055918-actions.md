Dependencies are fully up to date. Here's the summary:

**What changed:**
- `regex` 2026.4.4 → 2026.5.9 (patch update, locked in `uv.lock`)

**Quality gates — all green:**
- ✅ **Lint** (`flake8`): 0 issues
- ✅ **Tests**: 227 passed in 1.60s
- ✅ **Security scan** (`bandit`): No issues identified
- ✅ **Dependency audit** (`pip-audit`): No known vulnerabilities

All other pinned packages were already at their latest compatible versions per the constraint ranges in `pyproject.toml`.