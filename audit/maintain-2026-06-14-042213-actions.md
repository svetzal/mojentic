All done. Here's a summary of what was updated:

| Package | From | To | Type |
|---|---|---|---|
| `filelock` | 3.29.3 | 3.29.4 | patch |
| `pytest` | 9.0.3 | 9.1.0 | minor |

`pydantic-core` was already at the latest compatible version (2.47.0 — it had been updated in a prior run per the git log). All 279 tests pass, lint is clean, and no known vulnerabilities found. Changes committed and pushed to `main`.